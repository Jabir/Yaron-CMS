<% if @board && @board.errors && @board.errors.size > 0 %>
  <%= error_messages_for(:board) %>
<% elsif !request.xhr? %>
  <script type="text/javascript" charset="utf-8">
    Ext.onReady(function(){
      window.location = '<%= admin_boards_path(@site, @section) %>';
    });
  </script>
<% else %>
  CRUD_OK
<% end %>