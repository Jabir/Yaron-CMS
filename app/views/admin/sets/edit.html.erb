<% if @set && @set.errors && @set.errors.size > 0 %>
	<%= error_messages_for(:set) %>
<% elsif !request.xhr? %>
	<script type="text/javascript" charset="utf-8">
		Ext.onReady(function(){
			window.location = '<%= admin_sets_path(@site, @section) %>';
		});
	</script>
<% else %>
	CRUD_OK
<% end %>