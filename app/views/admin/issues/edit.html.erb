<% if @issue && @issue.errors && @issue.errors.size > 0 %>
	<%= error_messages_for(:issue) %>
<% elsif !request.xhr? %>
	<script type="text/javascript" charset="utf-8">
		Ext.onReady(function(){
			window.location = '<%= admin_issues_path(@site, @section) %>';
		});
	</script>
<% else %>
	CRUD_OK
<% end %>