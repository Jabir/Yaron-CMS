<% if @article && @article.errors && @article.errors.size > 0 %>
  <%= error_messages_for(:article) %>
<% elsif !request.xhr? %>
  <script type="text/javascript" charset="utf-8">
    Ext.onReady(function(){
      window.location = '<%= admin_articles_path(@site, @section) %>';
    });
  </script>
<% else %>
  CRUD_OK
<% end %>